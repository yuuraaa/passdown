# AGENTS.md

このリポジトリで作業する AI エージェント向けの指示。

## プロジェクト

passdown は、人間と AI エージェントが Task を通じて協働するためのタスク協働基盤。目的・範囲・機能は [要件定義書 v1](docs/Requirements.md) に書いてある。作業の前に必ず読むこと。

## 現在の段階

要件定義（v1）とアーキテクチャ設計が確定し、実装の段階。[設計書](docs/Architecture.md) に未決の項目はない。

- 実装の前に決まっていないことが出てきたら、設計と同じ手順で決める。たたき台を作る → 未決事項を1つずつ決める
- 未決事項はエージェントが勝手に決めず、選択肢と推奨を示して人間に判断してもらう
- 実装が設計と食い違ったら、コードに合わせて設計書を直す。設計書を古いまま放置しない

## 開発環境

開発作業はコンテナの中で行う。

```bash
docker compose up -d
docker compose exec passdown bash
```

- `compose.yaml` と `Dockerfile.dev` は開発用。本番用のイメージは別に作る
- ホストの `./data` をコンテナの `/data` にバインドマウントし、DB とバックアップを置く（設計書 2.9）。`data/` は git の対象外
- ホスト側の公開ポートは既定で 3100（`PASSDOWN_DEV_PORT` で変えられる）。コンテナの中では 3000 で待ち受ける
- 使う道具は設計書 2.8（npm・Drizzle 0.45 系・ESLint ＋ eslint-plugin-boundaries・Biome の formatter）

## ドキュメント

- `docs/Requirements.md` は要件定義書 v1。v1 でやること・やらないことだけを書いている
- 要件を変えるときは、要件定義書 5.3 の手順（シナリオを書く → 要件定義書を更新 → 設計への影響を確認 → 実装）を必ず通す
- 要件定義書のシナリオ（4章）にないものは作らない。「後で使うかもしれない」は理由にしない
- 将来のアイデアは要件定義書に書かない
- ドキュメントは日本語で書く

## 公開リポジトリとしての注意

このリポジトリは MIT ライセンスで公開する。

- 個人の情報（個人名・メールアドレス・自宅インフラの構成等）を公開するファイルに書かない
- 要件定義書では、使う人を特定の個人ではなく「オーナー」と書く

## Git

- GitHub Flow。`main` から `feature/` `fix/` `chore/` `docs/` `refactor/` などのブランチを切る（ケバブケース）
- コミットメッセージは gitmoji 形式（`<絵文字> <概要>`、概要は日本語）
  - ✨ 新機能 / 🐛 バグ修正 / ♻️ リファクタリング / 📝 ドキュメント / 🔧 設定 / 🗑️ 削除
- コミットはレビューしやすい単位に分ける
- push・PR 作成・マージは人間の許可を得てから行う
